import java.util.ArrayList;
import java.util.Collections;

public class NumberProcessor {

    // Method to sort the numbers
    public void sortNumbersAscending(ArrayList<Long> numbers) {
        Collections.sort(numbers);
    }
   
}
